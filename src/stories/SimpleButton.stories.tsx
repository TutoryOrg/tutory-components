import React from "react";

import { SimpleButton } from "../components/SimpleButton";

export default {
  title: "Custom/SimpleButton",
  component: SimpleButton,
} as ComponentMeta<typeof SimpleButton>;

export const SSimpleButton = () => {
  return <SimpleButton />;
};
